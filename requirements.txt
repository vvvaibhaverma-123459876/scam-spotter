# Full app dependencies (also used by the Hugging Face Space).
# The core library (rules, analyzer, calibration) is pure-stdlib; these power the
# DistilBERT classifier and the Gradio UI.
gradio>=4.44.0
torch>=2.2.0
transformers>=4.44.0
