# Hugging Face Space manifest (and local `pip install -r requirements.txt`).
# NOTE: when deployed as a Gradio Space, gradio itself is provided by the Space
# SDK (see `sdk_version` in README.md) — so it is intentionally NOT pinned here to
# avoid a version conflict. For a plain local UI install, use:
#     pip install -e ".[ml,app]"
torch>=2.2.0
transformers>=4.44.0
